已知 A为一串字符串，B为一个整数，Q为商，R为余数
//类似 100/2 ，商1/2=0，余1%2=1（商的第一位为0 0不输出），余数1与下一位进行组合 商（1*10+0=10）10/2=5 余10%2=0  在继续 商（0*10+0）/2=0
与（0*10+0=0）%2=0  此时字符串输出完毕
string s,ans;
int d=0,n,current;
for(i=0;i<s.size();i++)
{
current=d*10+s[i]-'0';//s是字符串要进行处理
ans=current/n+'0';//商
d=current%n;//余
}
//商的第一位为0时不输出，当商为多位时 可以后移输出下一位
for(i=(ans[0]==0 && ans.size() ?1 :0; i<ans.size();i++)
{
cout <<ans[i];
}
cout <<d;

